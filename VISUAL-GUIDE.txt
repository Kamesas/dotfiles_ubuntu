╔══════════════════════════════════════════════════════════════════════════╗
║                  GNU STOW DOTFILES - VISUAL GUIDE                        ║
╚══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│ CONCEPT: How Stow Works                                                 │
└─────────────────────────────────────────────────────────────────────────┘

   📁 ~/dotfiles/                      📁 ~/  (Your Home)
   ├── bash/                           ├── .bashrc ──────────┐
   │   └── .bashrc ←────────────────────────────────────────┘
   ├── git/                            │   (symlink!)
   │   └── .gitconfig ←───────────── .gitconfig ─────────┐
   └── nvim/                           │                    │
       └── .config/                    └── .config/         │
           └── nvim/ ←──────────────────── └── nvim/ ──────┘
               └── init.lua                      └── init.lua
                                                    (symlink!)

   ⚡ Edit in ~/dotfiles/  →  Changes reflect instantly in ~/

┌─────────────────────────────────────────────────────────────────────────┐
│ YOUR WORKFLOW                                                            │
└─────────────────────────────────────────────────────────────────────────┘

   1️⃣  FIRST TIME SETUP
   ┌─────────────────────────────────────────────────────────────────┐
   │ $ sudo apt install stow                                         │
   │ $ cd ~/dotfiles                                                 │
   │ $ ./migrate-to-stow.sh     ← Organizes your dotfiles           │
   │ $ ./install.sh              ← Creates symlinks                 │
   └─────────────────────────────────────────────────────────────────┘

   2️⃣  DAILY USE
   ┌─────────────────────────────────────────────────────────────────┐
   │ $ nvim ~/dotfiles/bash/.bashrc    ← Edit your configs          │
   │ $ source ~/.bashrc                 ← Changes are live!         │
   │ $ cd ~/dotfiles                                                 │
   │ $ git add -A && git commit -m "Update bashrc"                  │
   │ $ git push                         ← Sync to cloud             │
   └─────────────────────────────────────────────────────────────────┘

   3️⃣  ON NEW MACHINE
   ┌─────────────────────────────────────────────────────────────────┐
   │ $ git clone <your-repo> ~/dotfiles                             │
   │ $ cd ~/dotfiles                                                 │
   │ $ ./install.sh              ← Everything set up instantly!     │
   └─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ BACKUP SYSTEM                                                            │
└─────────────────────────────────────────────────────────────────────────┘

   📦 Backups: ~/.dotfiles-backups/
   
   ┌──────────────────────────────────────────────────────────────┐
   │  CREATE    │  ./backup.sh create                             │
   │  LIST      │  ./backup.sh list                               │
   │  RESTORE   │  ./backup.sh restore backup_20240101_120000     │
   │  CLEAN     │  ./backup.sh clean 10                           │
   └──────────────────────────────────────────────────────────────┘

   ⚠️  Automatic backup created before every installation!

┌─────────────────────────────────────────────────────────────────────────┐
│ PACKAGE MANAGEMENT                                                       │
└─────────────────────────────────────────────────────────────────────────┘

   📦 INSTALL ALL PACKAGES
   ┌──────────────────────────────────────────────────────────────┐
   │ $ ./install.sh                                               │
   └──────────────────────────────────────────────────────────────┘

   📦 INSTALL SPECIFIC PACKAGES
   ┌──────────────────────────────────────────────────────────────┐
   │ $ ./install.sh install bash git nvim                         │
   └──────────────────────────────────────────────────────────────┘

   🗑️  UNINSTALL
   ┌──────────────────────────────────────────────────────────────┐
   │ $ ./install.sh uninstall        ← Remove all                │
   │ $ ./install.sh uninstall nvim   ← Remove specific           │
   └──────────────────────────────────────────────────────────────┘

   📋 CHECK STATUS
   ┌──────────────────────────────────────────────────────────────┐
   │ $ ./install.sh status           ← See what's linked         │
   │ $ ./install.sh list             ← List packages             │
   └──────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ DIRECTORY STRUCTURE                                                      │
└─────────────────────────────────────────────────────────────────────────┘

   📁 ~/dotfiles/
   ├── 📄 backup.sh           ← Backup utility
   ├── 📄 install.sh          ← Main installer
   ├── 📄 migrate-to-stow.sh  ← Migration helper
   ├── 📄 README.md           ← Full docs
   ├── 📄 SETUP-GUIDE.md      ← Quick start
   ├── 📄 SUMMARY.txt         ← Command reference
   ├── 📄 .gitignore          ← Git ignore rules
   │
   ├── 📦 bash/               ← Shell configs
   │   ├── .bashrc
   │   ├── .bash_logout
   │   └── .profile
   │
   ├── 📦 git/                ← Git configs
   │   ├── .gitconfig
   │   └── .gitmessage
   │
   ├── 📦 nvim/               ← Neovim
   │   └── .config/
   │       └── nvim/
   │           └── init.lua
   │
   ├── 📦 tmux/               ← Tmux
   │   └── .config/
   │       └── tmux/
   │           └── tmux.conf
   │
   └── 📦 wezterm/            ← Terminal
       └── .wezterm.lua

┌─────────────────────────────────────────────────────────────────────────┐
│ ADDING NEW CONFIGS                                                       │
└─────────────────────────────────────────────────────────────────────────┘

   Example: Add Alacritty terminal config

   1️⃣  Create package structure:
   ┌──────────────────────────────────────────────────────────────┐
   │ $ cd ~/dotfiles                                              │
   │ $ mkdir -p alacritty/.config/alacritty                       │
   └──────────────────────────────────────────────────────────────┘

   2️⃣  Copy your config:
   ┌──────────────────────────────────────────────────────────────┐
   │ $ cp ~/.config/alacritty/alacritty.yml \                     │
   │      alacritty/.config/alacritty/                            │
   └──────────────────────────────────────────────────────────────┘

   3️⃣  Stow it:
   ┌──────────────────────────────────────────────────────────────┐
   │ $ stow alacritty                                             │
   └──────────────────────────────────────────────────────────────┘

   4️⃣  Commit to git:
   ┌──────────────────────────────────────────────────────────────┐
   │ $ git add alacritty                                          │
   │ $ git commit -m "Add alacritty config"                       │
   │ $ git push                                                   │
   └──────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ NIRI COMPATIBILITY                                                       │
└─────────────────────────────────────────────────────────────────────────┘

   ✅ Works perfectly with Niri (and any other window manager)!
   
   When you install Niri later:
   
   ┌──────────────────────────────────────────────────────────────┐
   │ $ mkdir -p ~/dotfiles/niri/.config/niri                      │
   │ $ # Copy Niri config when ready                              │
   │ $ stow niri                                                  │
   │ $ git add niri && git commit -m "Add Niri config"            │
   └──────────────────────────────────────────────────────────────┘

   Your existing dotfiles (bash, nvim, etc.) continue working! 🎉

┌─────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                          │
└─────────────────────────────────────────────────────────────────────────┘

   ⚠️  CONFLICT ERROR
   ┌──────────────────────────────────────────────────────────────┐
   │ $ ./backup.sh create                                         │
   │ $ rm ~/.bashrc          ← Remove conflicting file           │
   │ $ stow bash             ← Try again                          │
   └──────────────────────────────────────────────────────────────┘

   🔗 BROKEN SYMLINK
   ┌──────────────────────────────────────────────────────────────┐
   │ $ cd ~/dotfiles                                              │
   │ $ stow -R bash          ← Restow package                     │
   └──────────────────────────────────────────────────────────────┘

   ↩️  UNDO EVERYTHING
   ┌──────────────────────────────────────────────────────────────┐
   │ $ ./backup.sh list                                           │
   │ $ ./backup.sh restore backup_20240101_120000                 │
   └──────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ QUICK REFERENCE                                                          │
└─────────────────────────────────────────────────────────────────────────┘

   📚 READ THIS FIRST:  SETUP-GUIDE.md
   📖 FULL DOCS:        README.md
   📝 QUICK REF:        SUMMARY.txt
   🎨 VISUAL:           This file!

   🔗 Stow Basics:
      stow bash        Install bash package
      stow -D bash     Uninstall bash package
      stow -R bash     Reinstall bash package
      stow -n bash     Dry run (show what would happen)
      stow -v bash     Verbose output

╔══════════════════════════════════════════════════════════════════════════╗
║  🎉 Your dotfiles are now professional-grade and ready for Niri!        ║
╚══════════════════════════════════════════════════════════════════════════╝
