exec-once = blueman-applet
exec-once = ags
exec-once = $scriptsDir/KeybindsLayoutInit.sh

# ============================================================================
# CUSTOM STARTUP - Restored from Omarchy setup
# ============================================================================
# Laptop monitor state check on startup (ensures correct state based on lid position)
# Sleep 2 seconds to allow Hyprland to fully initialize and detect monitors
exec-once = sleep 2 && $HOME/.config/hypr/scripts/lid-switch.sh
